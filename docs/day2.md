🎣 SQLD [Day 2] WHERE 절 완전 정복 퀴즈
문항 1. SQL 문장의 논리적 실행 순서로 올바른 것은?

SELECT → FROM → WHERE

FROM → SELECT → WHERE

FROM → WHERE → SELECT

WHERE → FROM → SELECT

힌트: 재료를 먼저 가져오고(FROM), 손질한 뒤(WHERE), 접시에 담습니다(SELECT).

정답: 3)

오답노트: 작성 순서와 달리 컴퓨터는 데이터를 먼저 찾고 필터링한 뒤 최종적으로 보여줄 컬럼을 정합니다.

문항 2. 다음 중 WHERE 절에서 사용할 수 없는 것은?

비교 연산자 (=, <>)

SELECT 절에서 정의한 컬럼의 별칭(Alias)

컬럼명

리터럴 값 (숫자, 문자 등)

힌트: 별칭은 SELECT 단계에서 부여되는데, WHERE는 그보다 먼저 실행됩니다.

정답: 2)

오답노트: WHERE 절 실행 시점에는 SELECT 절의 별칭을 아직 인식하지 못하기 때문에 에러가 발생합니다.

문항 3. '급여가 3000이 아닌 사원'을 찾기 위해 WHERE 절에 쓸 수 있는 비교 연산자로 옳지 않은 것은?

SAL <> 3000

SAL != 3000

SAL ^= 3000

SAL ~= 3000

힌트: SQL에서 '같지 않다'는 표기법은 여러 개가 있지만, 물결 모양은 사용하지 않습니다.

정답: 4)

오답노트: SQL에서 다르다는 표현은 <>, !=, ^=가 표준 혹은 널리 쓰이는 방식입니다.

문항 4. WHERE 1 = 1 이라는 조건의 의미로 가장 적절한 것은?

첫 번째 컬럼의 값이 1인 데이터를 찾는다.

항상 참(True)이 되어 모든 행을 출력한다.

데이터 중 숫자 1이 포함된 행만 필터링한다.

문법 오류가 발생한다.

힌트: 1과 1은 항상 같습니다.

정답: 2)

오답노트: 조건이 항상 참이므로 필터링 없이 전체 데이터를 보여주며, 실무에서 쿼리 수정을 편하게 하기 위해 자주 씁니다.

문항 5. 데이터에 직업이 'MANAGER'라고 저장되어 있을 때, 다음 중 결과가 출력되는 쿼리는?

WHERE JOB = 'manager'

WHERE JOB = 'Manager'

WHERE JOB = 'MANAGER'

WHERE JOB = ' MAN AGER '

힌트: SQL 명령어는 대소문자를 안 가리지만, '데이터 값'은 매우 엄격합니다.

정답: 3)

오답노트: 문자열 데이터는 대소문자와 공백까지 정확히 일치해야 검색됩니다.

문항 6. WHERE SAL BETWEEN 2000 AND 3000의 결과와 동일한 조건은?

WHERE SAL > 2000 AND SAL < 3000

WHERE SAL >= 2000 AND SAL <= 3000

WHERE SAL >= 2000 OR SAL <= 3000

WHERE SAL > 2000 OR SAL < 3000

힌트: BETWEEN은 경계값(2000, 3000)을 포함할까요, 안 할까요?

정답: 2)

오답노트: BETWEEN A AND B는 '이상, 이하'의 개념으로 양쪽 끝 값을 모두 포함합니다.

문항 7. WHERE JOB IN ('SALESMAN', 'ANALYST')의 의미로 옳은 것은?

직업이 SALESMAN이면서 동시에 ANALYST인 사람

직업이 SALESMAN이거나 ANALYST인 사람

직업이 SALESMAN도 아니고 ANALYST도 아닌 사람

직업이 SALESMAN에서 ANALYST 사이의 범위에 있는 사람

힌트: IN은 여러 조건 중 하나라도 맞으면 OK인 'OR'의 집합입니다.

정답: 2)

오답노트: IN 연산자는 리스트에 나열된 값 중 하나라도 일치하면 데이터를 추출합니다.

문항 8. 이름이 'K'로 시작하는 모든 사원을 찾기 위한 조건은?

WHERE ENAME LIKE 'K_'

WHERE ENAME LIKE '%K'

WHERE ENAME LIKE 'K%'

WHERE ENAME LIKE '_K%'

힌트: 뒤에 글자 수가 몇 개든 상관없을 때 쓰는 와일드카드는 무엇일까요?

정답: 3)

오답노트: %는 0개 이상의 모든 문자를 의미하므로 'K%'는 K로 시작하는 모든 문자열을 찾습니다.

문항 9. WHERE ENAME LIKE '_A%' 조건이 의미하는 것은?

이름의 첫 글자가 A인 사람

이름의 두 번째 글자가 A인 사람

이름에 A가 포함된 모든 사람

이름이 두 글자이면서 A로 끝나는 사람

힌트: _(언더바)는 딱 한 글자 자리를 차지합니다.

정답: 2)

오답노트: 첫 번째 자리는 무엇이든 상관없고(_), 두 번째 자리가 A이며, 그 뒤는 상관없는(%) 데이터를 찾습니다.

문항 10. 커미션(COMM) 컬럼의 값이 비어 있는 데이터를 찾기 위한 올바른 문법은?

WHERE COMM = NULL

WHERE COMM IS NULL

WHERE COMM = ''

WHERE COMM IS EMPTY

힌트: NULL은 '값'이 아니라 '상태'이므로 비교 연산자 =를 쓸 수 없습니다.

정답: 2)

오답노트: NULL 여부를 확인할 때는 반드시 IS NULL 또는 IS NOT NULL을 사용해야 합니다.

문항 11. 논리 연산자의 우선순위가 높은 순서대로 나열된 것은?

AND > OR > NOT

OR > AND > NOT

NOT > AND > OR

NOT > OR > AND

힌트: 부정(NOT)이 가장 강하고, 그 다음이 교집합(AND)입니다.

정답: 3)

오답노트: 연산자 우선순위를 모르면 괄호 없는 복합 조건문에서 엉뚱한 결과를 얻을 수 있습니다.

문항 12. WHERE DEPTNO = 10 OR DEPTNO = 20 AND SAL > 2000 조건의 해석으로 옳은 것은?

10번 부서이거나 20번 부서인 사람 중 급여가 2000 초과인 사람

10번 부서인 사람 전체 + (20번 부서이면서 급여가 2000 초과인 사람)

10번 또는 20번 부서이면서 급여가 2000 이하인 사람

모든 부서원 중 급여가 2000 초과인 사람

힌트: AND가 OR보다 먼저 계산된다는 점에 주의하세요!

정답: 2)

오답노트: 우선순위에 따라 (DEPTNO = 20 AND SAL > 2000)이 먼저 묶이고, 여기에 10번 부서 조건이 OR로 결합됩니다.

문항 13. 위 12번 문제에서 '10번 또는 20번 부서원 중에서 급여가 2000 초과인 사람'을 뽑으려면 어떻게 수정해야 할까?

WHERE DEPTNO IN (10, 20) OR SAL > 2000

WHERE DEPTNO = 10 AND DEPTNO = 20 OR SAL > 2000

WHERE (DEPTNO = 10 OR DEPTNO = 20) AND SAL > 2000

수정할 필요 없다.

힌트: 먼저 처리하고 싶은 조건은 수학처럼 '이것'으로 묶어줍니다.

정답: 3)

오답노트: 괄호를 사용하면 우선순위를 강제로 지정하여 의도한 로직을 구현할 수 있습니다.

문항 14. 다음 중 WHERE 절의 역할에 대한 설명으로 틀린 것은?

테이블의 가로줄(행)을 필터링한다.

불필요한 데이터를 제거하여 시스템 부하를 줄인다.

SELECT 절보다 늦게 실행되어 별칭 사용이 자유롭다.

비교, 논리, SQL 특수 연산자를 활용할 수 있다.

힌트: 실행 순서 문제를 다시 떠올려 보세요.

정답: 3)

오답노트: WHERE는 SELECT보다 먼저 실행되므로 별칭을 사용할 수 없습니다.

문항 15. WHERE SAL NOT BETWEEN 1000 AND 2000의 의미는?

급여가 1000 미만이거나 2000 초과인 경우

급여가 1000 이상 2000 이하인 경우

급여가 딱 1000이거나 2000인 경우

급여가 1500인 경우만 제외

힌트: BETWEEN 결과(1000~2000)를 NOT으로 뒤집어 보세요.

정답: 1)

오답노트: NOT은 조건의 결과를 반대로 바꾸므로, 해당 범위에 포함되지 않는 데이터를 찾습니다.

문항 16. 이름에 'A'가 들어가지 않는 사람을 찾는 조건은?

WHERE ENAME LIKE '%A%'

WHERE ENAME NOT LIKE '%A%'

WHERE ENAME <> '%A%'

WHERE ENAME IS NOT 'A'

힌트: 패턴 검색의 부정은 NOT LIKE를 씁니다.

정답: 2)

오답노트: LIKE 앞에 NOT을 붙여 패턴에 해당하지 않는 행을 필터링합니다.

문항 17. WHERE MGR IS NOT NULL의 의미는?

관리자(MGR)가 없는 사원을 찾는다.

관리자(MGR)가 있는 사원(값이 비어있지 않은 사원)을 찾는다.

관리자 이름이 NULL인 사원을 찾는다.

오류가 발생하는 문장이다.

힌트: NULL이 아닌 상태를 찾는 조건입니다.

정답: 2)

오답노트: IS NOT NULL은 해당 컬럼에 어떤 값이든 채워져 있는 경우를 의미합니다.

문항 18. 다음 중 결과가 다른 하나는?

WHERE DEPTNO = 10 OR DEPTNO = 20

WHERE DEPTNO IN (10, 20)

WHERE DEPTNO ANY (10, 20) (참고: 실무적 표현)

WHERE DEPTNO BETWEEN 10 AND 20

힌트: 10과 20 사이에는 11, 12... 등의 숫자가 있을 수 있다는 점을 생각하세요.

정답: 4)

오답노트: BETWEEN은 10과 20 사이의 모든 값을 포함하지만, 1, 2번은 딱 10 또는 20인 경우만 찾습니다.

문항 19. SELECT 절에서 SAL * 12 AS 연봉이라고 했을 때, WHERE 절에서 연봉이 5000 이상인 데이터를 찾는 올바른 방법은?

WHERE 연봉 >= 5000

WHERE SAL * 12 >= 5000

WHERE AS 연봉 >= 5000

방법이 없다.

힌트: 별칭을 못 쓴다면 원래의 '식'을 그대로 가져와야 합니다.

정답: 2)

오답노트: WHERE 절에서는 별칭 인식이 안 되므로 계산식이나 원래 컬럼명을 직접 써야 합니다.

문항 20. WHERE ENAME LIKE 'S_T%' 조건으로 검색될 수 있는 이름은?

ST

SCOTT

SET

SATURN

힌트: S(첫글자) + 아무거나 한 글자(_) + T(세번째) + 그 뒤는 자유(%)

정답: 3), 4) (중복 가능하나 원리에 집중!)

오답노트: SET은 S+E+T이고 뒤에 %가 0개이므로 가능, SATURN은 S+A+T+URN이므로 가능합니다. SCOTT은 세 번째 글자가 O라서 안 됩니다.